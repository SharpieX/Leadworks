# Leadworks

npm install

bower install

#create index in mongodb

db.getCollection('books').createIndex({ title: 'text', author: 'text', description: 'text', isbn: 'text', genre:'text'})

npm run start // http://localhost:9000/login

npm run test //to run the test cases 

